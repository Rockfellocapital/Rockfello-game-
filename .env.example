# Sauvegarde cloud (optionnelle). Copie ce fichier en `.env.local` et remplis
# les valeurs depuis Supabase → Project Settings → API.
# SANS ces clés, le jeu fonctionne normalement en sauvegarde locale uniquement.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
